# Bartender Track and Trace Simulator — Backlog

Same format as ChefMate/ChefCellar: items `BL-XX`, priority P0 (MVP)/P1/P2, indicative size (XS/S/M/L). Check off (`- [x]`) once delivered, with a completion note.

## 0. Decisions to make before starting (blocking the rest of the backlog)

- [x] **BL-000** — Product name: **Bartender Track and Trace Simulator**, decided 2026-08-24. Repo stays `collector-simulator` as the technical identifier. See `CHARTE-GRAPHIQUE.md`.
- [x] **BL-001** — Framework stack confirmed by precedent 2026-08-24: Next.js (App Router, TypeScript) + Prisma + PostgreSQL + Tailwind + NextAuth v4 (`@auth/prisma-adapter`), same libraries as Supplier Connect. App skeleton built 2026-08-25 (see section 1-3 items below).
- [ ] **BL-002** — Bartender Track & Trace API scope: still open overall, but **first concrete API provided 2026-08-27** — the Locations (sites) API, see section 8 (BL-032/BL-033) and `CLAUDE-CONCEPT.md` section 7.2. Full scope (what else besides locations) still to come incrementally from Luc.
- [x] **BL-003** — Core entity model resolved 2026-08-30: Devices (section 5, done), Workflows as a graph of Tasks connected by Flow Links (section 6 below, `CLAUDE-CONCEPT.md` section 16), serialized-item identity is **EPC/SGTIN-96** (Luc's explicit confirmation). Breakdown in section 6.
- [ ] **BL-004** — Target users and access model: who uses this (Luc only, solution engineers, QA/dev team, sales demos)? Single-user tool or does it need accounts/auth?
- [x] **BL-005** — Infra fully set up 2026-08-25: GitHub repo `lbk-3344/collector-simulator` with `main` (Production) and `staging` (Preview) branches — an early mistake where only `staging` existed (making it GitHub's default and Vercel's Production Branch by mistake) was caught and fixed. Vercel project `collector-simulator` on the "ChefMate" team, Production Branch correctly set to `main`. Neon project `collector-simulator` (`lucky-lab-84095258`) with three branches: `production`, `staging`, `dev` (local-only). All env vars set in Vercel per environment, including per-environment `NEXTAUTH_URL` (branch-stable Preview URL / Production URL, not `localhost`) and matching Google OAuth redirect URIs. Local `.env` has the `dev`-branch `DATABASE_URL`, Google OAuth client, `NEXTAUTH_SECRET`, `INITIAL_ADMIN_EMAILS=lbellissard@seagullsoftware.com`. Ready for Claude Code to pick up the dev loop.

## 1. Design system (P0 — decided, implementation pending stack)

Decided 2026-08-24 in Cowork, inspired by Supplier Connect (`../supplier-portal`). Full rationale in `CHARTE-GRAPHIQUE.md`; token values and component previews in `style-guide.html`.

- [x] **BL-014** — Palette: orange (`#E8472A`, brand/logo/eyebrows) + white/warm-neutral as main colors, Seagull navy (`#0D1E2C`, buttons/links/focus) as contrast — a deliberate role-swap from Supplier Connect, where orange is the button color. Semantic colors (success/warning/danger/info) reused verbatim from Supplier Connect for cross-product consistency.
- [x] **BL-015** — Logo: reuse the official BarTender mark unaltered (`public/brand/bartender-logo.png`, copied from `supplier-portal`) — icon + real-text wordmark, same pattern as `BartenderLogo.tsx`, not a redraw.
- [x] **BL-016** — Typography: IBM Plex Sans + IBM Plex Sans Condensed + IBM Plex Mono (the mono face specifically for device IDs/EPCs/GTINs/timestamps), replacing Supplier Connect's Open Sans.
- [x] **BL-017** — Done 2026-08-25: CSS custom properties (light+dark tokens), IBM Plex fonts, and the button/card/chip/field component classes ported from `style-guide.html` into `app/globals.css`/`tailwind.config.ts` during scaffolding.
- [ ] **BL-018** — Confirm with Luc whether dark mode (included in `style-guide.html` from the start) is a real requirement or exploratory only — Supplier Connect doesn't have one today.
- [x] **BL-030** — Design system revision, decided 2026-08-26 in Cowork (supersedes part of BL-014/BL-016), implemented 2026-08-27: typography changed to **Inter + JetBrains Mono** (self-hosted via `@fontsource/inter`/`@fontsource/jetbrains-mono` v5.3.0, replacing IBM Plex Sans/Condensed/Mono — the condensed face is gone entirely, eyebrows/labels now just use Inter with uppercase+letter-spacing); page background changed from warm `#FAF8F4` to plain light gray `#F4F4F5`; orange's interactive role expanded to sidebar active-item fill, avatar-menu item hover (Sign out kept its red hover — destructive action, not a category), a new `.chip-brand` category-badge variant, and secondary/ghost buttons — navy still owns primary buttons/links/focus, semantic status colors untouched. `style-guide.html` regenerated against the new tokens (also picked up a pre-existing missing-`<meta charset>` bug found along the way, causing `→` to mojibake). See `CHARTE-GRAPHIQUE.md`.
- [x] **BL-031** — App shell layout revised, decided 2026-08-27 per Luc's explicit request, correcting BL-028, implemented same day: top bar simplified to logo + software name only (left) and the avatar/user menu only (right) — page title moves into each page's own content-area heading (`.page-title`, added to Overview; Settings already had its own; the three still-placeholder pages keep their existing eyebrow tag). Left sidebar relocated to sit below the top bar (full-width top bar; sidebar + content side by side underneath) and made retractable via a toggle button — collapsed shows icon-only nav items (centered, with a title-attribute tooltip), expanded shows icon+label, active item as a filled-orange pill in both states. Collapse state is in-memory only (resets on full reload), not persisted. See `CHARTE-GRAPHIQUE.md` "App shell layout".
- [x] **BL-035** — Done 2026-08-27, decided per Luc's explicit request: replaced the plain colored-text pattern used for save/error/test-connection messages with a new `.snack`/`.snack-success`/`.snack-warning`/`.snack-danger` component — solid semantic-color background (not the existing light tint), text color chosen per-color for WCAG AA contrast (navy on success/warning, white on danger). See `CHARTE-GRAPHIQUE.md` "Inline feedback (snacks)". Applied to `BartenderConnectionTab.tsx` (Saved / Test connection result), the `.error-banner` usages on `UsersTable.tsx`, `(auth)/login/page.tsx`, `BugReportModal.tsx`, and `BugReportsTable.tsx` (BL-034 landed after the spec doc was written, so it wasn't on the original list — caught it via grep before assuming full replacement, per the task's own instruction). `.error-banner` class definition left in place in `globals.css`, unused for now.

## 2. Authentication & user management (P0 — decided, "très similaire à Supplier Portal")

Decided 2026-08-24 in Cowork. Full spec, roles, and flow diagram in `CLAUDE-CONCEPT.md` section 4. Two-role model (`ADMIN`/`USER`) plus a `PENDING` gate — simpler than Supplier Connect's brand/supplier split since this tool has no such distinction.

- [x] **BL-019** — Done 2026-08-25: `User` model + standard NextAuth tables (`Account`, `Session`, `VerificationToken`) via `@auth/prisma-adapter`; `Role` enum (`ADMIN`/`USER`/`PENDING`). `prisma/schema.prisma`.
- [x] **BL-020** — Done 2026-08-25: NextAuth v4 setup, `GoogleProvider` only, `lib/auth.ts` mirroring Supplier Connect's structure.
- [x] **BL-021** — Done 2026-08-25, with one deviation from the original plan: the domain/bootstrap-admin role resolution runs in the **`jwt` callback**, not `signIn` — the Prisma adapter only persists a brand-new user *after* `signIn` resolves, so resolving the role inside `signIn` would race the adapter's own `createUser()`. The `jwt` callback resolves it right after the row is guaranteed to exist, only ever moves a user *out of* `PENDING` (never overwrites an admin-assigned role), and re-reads the DB on every request — which is also what makes an admin's Validate/role-change take effect without the user re-logging in.
- [x] **BL-022** — Done 2026-08-25: `/auth/pending` waiting page, polling `/api/auth/status` every 30s.
- [x] **BL-023** — Done 2026-08-25: middleware requiring auth on every route except `/login`/`/auth/pending`, redirecting `PENDING` users to the waiting page.
- [x] **BL-024** — Done 2026-08-25: `/settings` shell. Non-admins currently see a plain "Nothing here yet" placeholder (no tabs) rather than a tab list with Users hidden — same end result (no admin functions visible), simpler until a second tab exists.
- [x] **BL-025** — Done 2026-08-25, with one simplified field set: table shows name/email/avatar/role/joined only — **no auth-provider or last-login columns**, since those aren't on the `User` model yet. Add `authProvider`/`lastLoginAt` to the schema first if those are wanted. Inline role switch, **Validate** for `PENDING`, **Delete** with confirmation — all working.
- [x] **BL-026** — Done 2026-08-25: last-remaining-`ADMIN` can't be deleted or demoted — enforced both in the UI (disabled + tooltip) and server-side in the API routes.
- [ ] **BL-027** — Nice-to-have, inspired by Supplier Connect's `AdminNotification`: a pending-count badge on the Users tab. Not built yet. *(XS)*
- [x] **BL-028** — Done 2026-08-25: app-shell layout built from `mockup-app-shell.html` — left sidebar (Overview/Devices/Workflows/Serialized Items only), top bar with user avatar menu (Settings, Report a bug, Sign out). Mobile/tablet layout still intentionally deferred.
- [x] **BL-032** — Per-user Bartender connection settings, decided 2026-08-27, implemented same day: new **Bartender Connection** tab in Settings, open to **every role including `PENDING`** (not admin-gated, unlike Users) — each user enters their own Bartender tenant URL + API key. See `CLAUDE-CONCEPT.md` section 7.1. `User.bartenderTenantUrl`/`bartenderApiKeyCiphertext`/`bartenderApiKeyLast4` (migration `add_bartender_connection_settings`), `ENCRYPTION_KEY` (AES-256-GCM) generated and set in local `.env` — **still needs setting in both Vercel scopes (staging/production) before this works there**, `lib/crypto.ts`, `GET`/`POST /api/settings/bartender`, and `BartenderConnectionTab.tsx`. Since `PENDING` now needs `/settings`, `middleware.ts` and `app/(app)/layout.tsx` were both adjusted to let `PENDING` reach `/settings` and `/api/settings/*` specifically while staying blocked everywhere else — see `CLAUDE-CONCEPT.md` section 13.
- [x] **BL-033** — "Test connection" action + first live Bartender call, decided 2026-08-27, implemented same day: button in the Bartender Connection tab calling `GET {tenantUrl}/statemachine-api-configuration/rest/configuration/locations?level=premise` (header `apikey: <key>`) server-side via `POST /api/settings/bartender/test` — the key never reaches the browser's network tab. Tested live against Luc's sandbox tenant (33 locations); response shape (plain JSON array of location objects) and error behavior (401 + plain-text `Unauthorized` for a bad key) now documented in `CLAUDE-CONCEPT.md` section 7.2.

## 3. Bug reporting (P0 — build now, mirroring ChefMate 1:1)

Brought forward from "later" to now — Luc flagged it as genuinely useful. See `CLAUDE-CONCEPT.md` section 3 and `CLAUDE.md` "Bug handling" for the full spec/process.

- [x] **BL-006** — Done 2026-08-25: `BugReport` Prisma model — title, description, reporter relation, status (`OPEN`/`RESOLVED`), optional `screenshotUrl`, reported/resolved timestamps.
- [x] **BL-007** — Done 2026-08-25: in-app bug report modal (title, description) opened from "Report a bug" in the avatar menu, posting to `/api/bugs`. Screenshot upload is a disabled placeholder button for now — wiring it up is BL-008.
- [x] **BL-034** — Done 2026-08-27, explicit request from Luc: admin-only **Bug Reports** tab in Settings, listing currently `OPEN` bugs (title, reporter, reported date) with a pending-count-style badge on the tab, and a detail view (title, reporter, full description, screenshot if `screenshotUrl` is set — always empty for now until BL-008 lands). Read-only — doesn't touch status or the resolve/notify flow (`CLAUDE.md` "Bug handling" stays the CLI-driven process it already is). `GET /api/bugs` (previously an open-to-any-signed-in-user stub) is now admin-gated to power it.
- [ ] **BL-008** — Screenshot storage: reuse Cloudinary the same way ChefMate/ChefCellar do, pending confirmation this work project can use the same account (see BL-001). *(S)*
- [ ] **BL-009** — `npm run bugs:export` script → regenerates `BUGS.md` from all `OPEN` `BugReport`s. *(S)*
- [ ] **BL-010** — `npm run bugs:notify-start -- <id>` and `npm run bugs:notify-resolved -- <id>` scripts, with Resend email templates ("being resolved" / "resolved") to the reporter; `notify-resolved` also deletes the row after sending. *(M)*
- [ ] **BL-011** — `SOLVED-BUGS.md` archive step (append on `RESOLVED`, same fields as ChefMate: number, title, description, dates, screenshot URL if present). *(XS)*
- [ ] **BL-012** — Target `DATABASE_URL_PRODUCTION` specifically for export/notify scripts once staging/main environments exist (BL-005). *(XS)*
- [ ] **BL-013** — Confirm who a "reporter" is for this tool (internal teammate vs. external client) — depends on BL-004; may simplify the email copy/flow versus ChefMate's household-facing wording.

## 4. Technical foundations

*(To break down once the rest of section 0 is settled — on the ChefMate model: Next.js/TypeScript scaffold, Prisma + Neon, staging/main Vercel deployment.)*

## 5. Devices

*(To break down once BL-001 and BL-003 are settled.)*

- [x] **BL-029** — Read-point type icon set integrated 2026-08-26: 13 line-art icons (`components/ui/ReadPointIcon.tsx`, `public/icons/read-point/`), see `CHARTE-GRAPHIQUE.md` "Iconography". Delivered by a separate Claude Design session (source in `package/`), keyed off a partial/observational `GET /reference/read-point-types` type-code list (see `CLAUDE-CONCEPT.md` section 7) — **not yet confirmed with Luc** whether "read-point" is the same concept as this backlog's "Device", or related-but-distinct. Feeds into BL-003 once settled.
- [x] **BL-036** — Done 2026-08-27: `Device` table (migration `add_device_and_selected_location`) exactly as decided — `id`/`name`/`type`/`locationCode`/`positionX`/`positionY`/`status`. Seeded via `npm run db:seed-devices` (`prisma/seed-devices.mjs`, checked in, not throwaway) with 9 fixture devices across 3 real location codes from Luc's sandbox tenant, fetched live via the already-working BL-033 endpoint: `TTMEMBASE` (dc), `TANDTWAREHOUSE` (store), `GRANITEFALLSSHOP` (dc) — mix of types (`PORTAL`/`CONVEYOR`/`SHELF`/`OVERHEAD`/`TABLETOP`/`SIMPLE_READER`/`DOORFRAME`) and `ONLINE`/`OFFLINE` statuses, positions in the 0-800 range as placeholders. `GET /api/devices?locationCode=X` verified working end-to-end (200, correct rows, correct counts).

- [x] **BL-042** — Done 2026-08-28: `Device` model redesign (Collector fields, single default Channel, `configured` flag) + new minimal `Workflow` model (`RUNNING`/`STOPPED`), see `CLAUDE-CONCEPT.md` section 15.1/15.2. Migration `redesign_device_add_workflow` drops the old `status`/`DeviceStatus` entirely. `prisma/seed-devices.mjs` rewritten and re-run: 2 Workflow fixtures (`Pack Line A` RUNNING, `Inbound QC` STOPPED) + 10 devices across the same 3 real sites, a deliberate mix of all 4 states (some unconfigured, some Active, some on each Workflow) so BL-043's coloring and BL-047's list both have real data. `app/api/devices/route.ts` extended: `GET` optional `locationCode` (site-scoped or tenant-wide), `POST` creates either a bare "shell" (map-drop, `configured: false`) or a full config (`name` present → `configured: true`, shared with PATCH via `lib/deviceConfig.ts`'s `buildDeviceConfigData`). Added `app/api/devices/[id]/route.ts` (`GET`/`PATCH`/`DELETE`) and `app/api/workflows/route.ts` (`GET`/`POST`, no dedicated authoring UI yet).
- [x] **BL-043** — Done 2026-08-28: `lib/deviceState.ts`'s `getDeviceState()` — pure, derives Off/Active/Automated/Problem from `configured` + `workflow?.status` per `CLAUDE-CONCEPT.md` section 15.3. `--device-*` tokens (light/dark/`data-theme`) added to `globals.css` verbatim from `CHARTE-GRAPHIQUE.md`. Applied as the marker icon's inline `color` on the Overview map (`LocationMapCard.tsx`) and as a dot+label (`.device-state*` classes) on the Devices list. "Devices online" KPI (`OverviewClient.tsx`) redefined as Active+Automated count.
- [x] **BL-044** — Done 2026-08-28: Edit-mode toggle added as a 5th `.map-controls` button on `LocationMapCard.tsx`. While on: a top-left `.device-palette` (mirrors the existing bottom-right widget's styling) lists all 13 read-point types as drag sources — pointer-based (`mousedown`/window-level `mousemove`/`mouseup`), not HTML5 DnD, per the spec's explicit recommendation. Dropping a palette icon on the map inverts the current pan/zoom transform to get floor-plan pixel coordinates, `POST`s a shell Device, and opens the BL-045 config modal. Existing markers become drag sources for reposition-only (`PATCH` position fields only) or, if not actually moved, open the config modal (click-to-configure) — distinguished by a small pixel-movement threshold. **Real bug found and fixed during live verification**: the drag-reposition handler originally read the final position from React state (`draggingDeviceVisual`) inside the `mouseup` handler — a stale-closure race, since `mouseup` can fire before React re-runs the effect with a fresh closure over the latest state (especially on a fast drag with few intermediate `mousemove`s), silently sending the device's *original* unmoved position instead of the dragged one. Fixed by tracking the current drag position synchronously on the drag ref itself, not in state. Caught by direct dispatched-`MouseEvent` testing after the browser-automation drag tool's own synthesis proved inconclusive.
- [x] **BL-045** — Done 2026-08-28: `components/DeviceConfigModal.tsx`, structural pattern copied from `BugReportModal.tsx`. All fields per `CHARTE-GRAPHIQUE.md` "Device config screen"; Read point type/Site are read-only (icon+label / plain text) when `lockTypeAndSite` (map contexts), editable selects otherwise (Devices list). Cancel deletes the underlying shell row only when `deleteOnCancelIfUnsaved` (a just-dropped, never-configured device) — editing an already-configured device leaves it untouched on Cancel, per spec. Workflow select populated live from `GET /api/workflows`.
- [x] **BL-046** — Done 2026-08-28: non-Edit-mode marker clicks in `LocationMapCard.tsx` route on `getDeviceState()` — Off opens the BL-045 config modal (pre-filled, same as the list's Edit), Active opens a minimal "Manual data send — coming soon" placeholder modal (no real send flow, per Luc's explicit deferral), Automated/Problem open a read-only info panel (Collector ID/type/state/workflow, no edit affordance). All 4 states live-verified against real seeded fixtures.
- [x] **BL-047** — Done 2026-08-28: `app/(app)/devices/page.tsx` replaced with a real tenant-wide table (`GET /api/devices` with no `locationCode`), pattern copied from `UsersTable.tsx`/`BugReportsTable.tsx`. Columns exactly per `CHARTE-GRAPHIQUE.md`: type icon+label, Name+Collector ID, Site (resolved via `GET /api/locations`), State (dot+label), Workflow (name or "—"), Edit/Delete. "+ Add device" and row Edit both open the BL-045 modal with `lockTypeAndSite: false`. Full CRUD round-trip (add/edit/delete) live-verified.

Along the way: `devices` state was lifted from `LocationMapCard.tsx` into `OverviewClient.tsx` (passed down as `devices`/`onDevicesChange` props) — it was originally fetched independently inside the map card, which meant Edit-mode changes (create/reposition/reconfigure) updated the map's own markers but left the "Devices online" KPI card stale until a full page reload. Single source of truth now fixes that: the map's own mutations patch the lifted state directly, no re-fetch needed, and the KPI updates immediately.

- [x] **BL-044a** — Done 2026-08-28, direct follow-up from Luc on the just-shipped BL-042–047 map/Edit-mode work: (1) device markers were invisible at the default zoomed-out "fit to screen" scale — root cause was `.map-card-transform`'s own zoom `scale()` (as low as ~0.14 on a large floor plan) shrinking the marker proportionally along with everything else inside it, not a z-index/stacking issue; fixed with a counter-scale (`scale(1/currentZoom)`) applied inline on each `.map-marker` in `LocationMapCard.tsx` so markers stay a constant on-screen size at any zoom level. (2) markers restyled per Luc's request: bigger (36×36px) circular badge, white icon on a colored background (`--device-*` token now drives `background` instead of icon `color`). (3) dragging a palette device type onto the map while pan mode was also on moved the map itself along with the not-yet-placed icon — the palette sits inside `.map-card-viewport`, so its `mousedown` was bubbling up to the viewport's own pan handler; fixed with `e.stopPropagation()` in `handlePaletteMouseDown`. (4) `DeviceConfigModal.tsx` now shows a big circular read-point-type icon top-left of the title, live-updating as the type selection changes. All four live-verified: markers visible/circular/white-on-color at default zoom, palette-drag-to-place no longer pans the map (confirmed the `.map-card-transform` value is byte-identical before/after the drag), config modal shows the new icon.

- [x] **BL-044b** — Done 2026-08-28, direct follow-up from Luc: the circle size on BL-044a's device markers was right, but the icon glyph inside it read too small on both the map markers and the Edit-mode device-type palette. Bumped the `ReadPointIcon` `size` prop (map marker 20→26px, palette items and the drag-ghost 22→28px) while leaving the 36px circle/box unchanged. Live-verified via zoomed screenshots on both the map and the palette.

- [x] **BL-044c** — Done 2026-08-28, second follow-up from Luc asking for the glyphs bigger still, plus consistency in two places BL-044b missed — the Devices list table and `DeviceConfigModal.tsx`'s two icon spots. Map marker 26→30px; palette/drag-ghost 28→32px (palette item box also grown 36→40px so the bigger glyph keeps breathing room); Devices list table icon 20→28px; the config modal's big top-left icon 26→32px and its read-only "locked type" row icon 22→26px. Live-verified via zoomed screenshots across all four locations (map, palette, list, modal).

- [x] **BL-049** — Done 2026-08-28: `Device.channelId/channelType/channelDirection/channelPresenceEvent/channelAttributes` (the v1 flattened single-Channel fields) replaced with a real repeatable `channels Json?` array (`{id, type, presenceEvent?, direction?}[]`), migration `add_device_channels_and_publish`. `DeviceConfigModal.tsx` gained a full Channels section: each row shows its `CH{n}` id (mono, not editable, renumbered by position), a Type icon-toggle (Presence/Directional), a second icon-toggle whose options depend on the row's type (First seen/Present/Last seen, or Inbound/Outbound), and a remove button disabled on the last remaining row. "+ Add channel" appends a new Presence/Present row. New `.icon-toggle`/`.icon-toggle-btn` CSS component. `lib/deviceConfig.ts`'s `buildDeviceConfigData`/`validateChannels` accept and minimally validate the array (400 if present-but-empty/malformed), defaulting silently to a single `CH1` row only when the field is absent entirely.
- [x] **BL-050** — Done 2026-08-28: new `GET /api/devices/suggest-code?locationCode=&type=` returns `{locationCode}-{type}-{NN}`, `NN` a 2-digit sequence counting existing Devices at that Site+Type pair (configured or shell, so numbers never collide). `DeviceConfigModal.tsx` calls it automatically whenever type/site change and Collector ID is empty, plus a small ghost "regenerate" icon button beside the field for an explicit re-pull — never overwrites a value already present except via that explicit click. Verified two `PORTAL` devices at `TTMEMBASE` produce `-01` then `-02` in sequence.
- [x] **BL-051** — Done 2026-08-28: `Device.publishedAt DateTime?` added (same migration as BL-049). `lib/deviceState.ts`'s Off/non-Off boundary now gates on `configured && publishedAt`, not `configured` alone — a fully configured, never-published Device reads as grey "Not configured" (live-verified with a re-seeded fixture). `buildDeviceConfigData` only ever *sets* `publishedAt` (`body.publish === true`), never clears it — editing an already-published Device leaves it published. `DeviceConfigModal.tsx`'s footer now branches on `device?.publishedAt`: unpublished shows Cancel/Save draft/Publish to platform (cloud-upload icon, primary), published shows just Cancel/Save. A live state pill (reusing the Devices list's `.device-state*` dot+label) sits next to the modal title, omitted for a brand-new device. Live-verified full round trip: Save draft leaves a new Device grey, Publish to platform flips it to Active immediately, re-editing a published Device shows the two-button footer and doesn't reset its state.

Along the way (Phase 5): `prisma/seed-devices.mjs` re-run with the new shape — every previously-configured fixture now also carries `publishedAt`, two Devices got 2-Channel lists mixing Presence and Directional, and one new fixture (`TTMEMBASE-OVERHEAD-01`, "Overhead Reader — Membase Staging") is `configured: true` but `publishedAt: null` specifically to demonstrate the new gate on both the Devices list and the Overview map.

- [x] **BL-049a** — Done 2026-08-28, direct follow-up from Luc right after BL-049 shipped: each Channel row now has an optional, freely editable Name (e.g. "Entry sensor") next to its fixed `CH{n}` id — same id/name split as Device's own `collectorId`/`name`. `DeviceChannel` (`lib/deviceConfig.ts`) gains `name?: string`; no schema migration needed since `channels` is a `Json` column. Restructured `.channel-row` in `DeviceConfigModal.tsx`/`globals.css` into a two-line layout (id + name input + remove button on top, type/sub-type toggles below) to fit the new field. Along the way, fixed a real bug caught during this pass: `setChannelType` was rebuilding the row from scratch on a Presence/Directional switch and silently dropping any `name` already typed — now preserves it. Live-verified: typing a name, switching the row's type, saving, and reopening the device all keep the name intact.

- [x] **BL-052** — Done 2026-08-29, direct request from Luc, three small polish items: (1) the Overview map's Edit-mode device-type palette (`.device-palette-item`) now shows a custom-styled hover tooltip with the type label (dark chip above the icon) instead of relying on the browser's own delayed/unstyled `title` tooltip — swapped `title` for `aria-label`+`data-tooltip` driven by a new CSS `::after` rule. (2) The Devices list's Edit/Delete row actions (`app/(app)/devices/page.tsx`) are now icon-only (`.row-icon-btn`): Edit is a pencil icon on a solid orange (`--accent-brand`) background with a white icon, Delete is a trash icon in the existing ghost/danger-on-hover treatment, both with `aria-label`/`title` for accessibility. (3) `.snack-success`'s text color changed from navy to white (`app/globals.css`) — supersedes part of BL-035's original WCAG-AA-contrast choice (navy was deliberately chosen there for contrast reasons, now overridden by explicit request); `.snack-warning` untouched. All three live-verified.

## Real platform publish & sync — added 2026-08-29

Direct request from Luc, see `CLAUDE-CONCEPT.md` section 15.8 (full spec) and section 7.5 (DataCollector API, corrected same day) and section 13's 2026-08-29 decision-log entry. **Supersedes** BL-042/BL-049's "no real Bartender DataCollector call" framing, for Publish/Delete only.

- [x] **BL-053** — Real "Publish to platform" + auto-resync. `POST /collectors/register` now fires for real on Publish and on every Save of an already-published Device (`lib/bartenderDataCollector.ts` client, `lib/deviceSync.ts` orchestration). `publishedAt` set only on a `2xx`; a failed call never blocks the local save and never un-publishes. New `Device.lastSyncedAt`/`lastSyncError`/`platformReconciliation` (migration `20260829155657_add_device_platform_sync_tracking`). `configVersion` version semantics in `resolveConfigVersion` (default `"1"`, auto-increment on a syncing save only when left unchanged and purely numeric; a hand-typed value always wins). Config modal shows a sync `.error-banner` and per-Channel CONFLICT/BROKEN flags; a failed publish also shows a `.snack-danger` and holds the modal open.
  **Phase 0 live-verified 2026-08-29** (sandbox tenant, staging DB connection — see CLAUDE-CONCEPT.md §15.8 "Phase 0 live verification"): (a) sandbox gateway `api.sandbox.bartender-tt.com/datacollector` confirmed working; (b) **API key is tenant-wide, not Location-scoped** — register succeeded at both `TTMEMBASE` and `TANDTWAREHOUSE` with the one key, matching Luc's expectation, `403 LOCATION_MISMATCH` risk retired; (c) `reconciliation` real shape is `{ affectedMappings: [{ channelId, mappingStatus, issue, detail, ... }] }` (spec's assumed shape was wrong); (d) **`heartbeatConfig` request shape unresolved** — 10+ shapes rejected with a generic type error, YAML not in repo → heartbeat config is **omitted from the register payload** for now, needs the schema from Luc. E2E (forged session → real routes → real sandbox) all green.
- [x] **BL-054** — Real "Deregister on delete", opt-in every time. Deleting a **published** Device asks a second `useDialog().confirm()` after the delete confirm ("Deregister too" / "Just delete here"); passes `?deregister=true` to `DELETE /api/devices/[id]`, which calls `DELETE /collectors/{collectorId}` before removing the local row (`404 COLLECTOR_NOT_FOUND` counts as success). Local row always deleted; a remote failure comes back as `platformDeregisterError` → `.snack-warning`. Never-published Devices keep the single-confirm local-only flow.

## 6. Workflows — Item Feeds, Tasks, Flow Links (BL-003 resolved 2026-08-30)

Full design in `CLAUDE-CONCEPT.md` section 16, discussed and confirmed with Luc 2026-08-30. **Supersedes** BL-042/15.2's placeholder `Device.workflowId` direct link — see BL-059 below. Mockups delivered by Cowork, not yet in the repo: `item-feeds-and-flow-links.html` (entity concepts), `workflow-canvas-editor.html` (interactive canvas/edge-config demo).

- [ ] **BL-055** — Legacy Product API client (`product-api`, section 7.7): `lib/bartenderProducts.ts` wrapping `GET /products`, `GET /products/{gtin}`, `GET /categories`, HTTP Basic auth (reuse BL-040's stored username/password). A GTIN/category picker component, used by BL-058's Item Feed form. Flag to Luc if `/products?showAttributes=true` genuinely has no search/filter param once tested live — it's documented as "can be huge." *(M)*
- [ ] **BL-056** — Legacy Serialization API client (`serialization-api`, section 7.6): `lib/bartenderSerialization.ts` wrapping `GET .../hexas/sgtin96/gtin/{gtin}?quantity={n}`, HTTP Basic auth. **Hard-coded 10-item cap enforced in this client itself**, not just at the call site — every call is a real, permanent write to the live platform. *(S)*
- [ ] **BL-057** — Inventory API client (`inventory-public-api`, section 7.8): `lib/bartenderInventory.ts` wrapping `GET /stock` (`groupBy=zone`). Auth pattern needs a live check first (spec implies Basic + `apiKey` header together, possibly `x-tenant` too — see section 7.8). *(M)*
- [ ] **BL-058** — `ItemFeed` Prisma model (kind `NEW`/`PRESENT`/`FIXED`, see 16.1) + CRUD API + a library list/create/edit UI (own page or a Workflows-section tab) — reusable across Tasks, not scoped to one Workflow. Depends on BL-055/056/057 for the picker and kind-specific fields. *(L)*
- [ ] **BL-059** — `Task`, `TaskChannelInput`, `FlowLink` Prisma models (16.2/16.3) replacing `Device.workflowId` with `Device` → `Task` → `Workflow` (16.6). `lib/deviceState.ts` updated to derive Automated/Problem via `device.task?.workflow?.status`. Re-seed fixtures rather than migrate row-for-row (same approach as BL-042/BL-049). No dedicated UI yet — CRUD API only, canvas comes in BL-060. *(L)*
- [ ] **BL-060** — Workflow canvas editor (16.4): `@xyflow/react`-based graph UI — Device palette (drag to create a Task node with one port per Channel), drag-to-connect between ports to create a Flow Link, click an edge to open its delay/filter config panel. Matches `workflow-canvas-editor.html`. Depends on BL-058/059. *(L)*
- [ ] **BL-061** — Run & automation engine (16.5): per-Task `fireIntervalSeconds`, in-flight batch simulation (firing → simulated read event → evaluate outgoing Flow Links → schedule next-hop arrivals), a Vercel Cron-triggered protected API route driving it independent of any logged-in session, and the per-Workflow `maxRunDurationMinutes` safety auto-stop (manual restart required after). **Phase 0, before building**: live-check Vercel Cron's minimum frequency on the actual plan in use — if it can't fire often enough for per-Task intervals, fall back to an external scheduler (e.g. cron-job.org) hitting the same protected route on a shared secret. **Needs a number from Luc**: default `maxRunDurationMinutes` isn't set yet — ship with a conservative placeholder and confirm. *(L)*

## 7. Serialized items

Covered by BL-056 (minting) and BL-058 (Item Feed's `FIXED`/explicit-EPC list) above — no separate standalone entity beyond what an Item Feed produces. Revisit if a dedicated "browse serialized items" view is wanted later.

## 8. Bartender API integration

*(To break down as each API is provided per BL-002 — one sub-section per API, mirroring how `CLAUDE-CONCEPT.md` section 7 documents them.)*

- Per-user connection settings (prerequisite for every API below) and the first API (Locations/sites) are tracked as **BL-032** and **BL-033** in section 2 above, since they live in Settings — see `CLAUDE-CONCEPT.md` section 7.1/7.2.
- **2026-08-27** — a second, much more complete `location-api-v2` OpenAPI spec was provided by Luc (added directly to the claude.ai Project) — full CRUD for Locations/Zones/Location Types/Zone Types/Channel Mappings/floor-plan upload, see `CLAUDE-CONCEPT.md` section 7.3. Chosen (over the already-tested `statemachine-api-configuration` endpoint) as the API the new site selector/map feature builds against — see section 9 below.
- **2026-08-27** — **corrected same day, now live-verified working**: the second gateway is `https://api.sandbox.bartender-tt.com` (**sandbox**, not staging as first documented — `https://api.staging.bartender-tt.com` was never a real endpoint), selected when `bartenderTenantUrl` contains `sandbox`; auth header is lowercase `apikey`, same convention as BL-032/033, not `X-API-Key`. Live-tested against Luc's sandbox tenant: `/locations` and `/locations/{code}/zones` both return real data (200); `/locations/{code}/map` returns `403` (permission-scope gap on this key, unrelated to the gateway/header fix — flagged for Luc, not something to fix in this app). `lib/bartenderLocations.ts` updated to the corrected gateway/header/response-envelope shapes — see `CLAUDE-CONCEPT.md` section 7.3 for full details.

- **2026-08-28** — `location-api-v2`'s `/locations/{code}/map` 403 (section 7.3's `Local Map[read]` permission gap) isn't resolvable from this app — Luc identified a temporary workaround via the older `statemachine-api-configuration` API, using a `level=floor` lookup plus a Basic-Auth-only `/maps` endpoint. See `CLAUDE-CONCEPT.md` section 7.4 and BL-040/BL-041 below.
- **2026-08-29** — full `datacollector-api-v3` spec re-read for the real-publish feature (BL-053/BL-054 above) — several corrections/additions to `CLAUDE-CONCEPT.md` section 7.5's earlier summary: the exact `CollectorRegistration` required-field list, the `403 LOCATION_MISMATCH` error definition (flagged as a real multi-site risk), the `DELETE /collectors/{collectorId}` deregister endpoint, and the `GET /collectors`/`GET /locations/{code}/collectors` listing endpoints (deliberately not used by this app).

## 8b. Temporary legacy map workaround (Basic Auth)

Added 2026-08-28 per Luc, see `CLAUDE-CONCEPT.md` section 7.4 for the full spec. Temporary — remove once `location-api-v2`'s own map endpoint (BL-038) is unblocked for the stored API key.

- [x] **BL-040** — Done 2026-08-28: Track & Trace Basic Auth credentials (Username/Password) added to Settings → Bartender Connection — a new, temporary third credential type. `User.bartenderUsername`/`bartenderPasswordCiphertext` (password encrypted like the API key via `lib/crypto.ts`, no masked preview — just "Set"/direct input like the API key's own pattern), migration `20260828103254_add_bartender_basic_auth`. `GET`/`POST /api/settings/bartender` extended (username follows `tenantUrl`'s always-write semantics, password follows the API key's only-write-when-typed semantics); `BartenderConnectionTab.tsx` gets two new fields plus a note explaining why a second credential type exists. Verified live: saves and reloads correctly, and saving tenant URL/API key alone still works unchanged.
- [x] **BL-041** — Done 2026-08-28: `LocationMapCard`'s map fetch falls back to the legacy floor-plan endpoint when `location-api-v2` returns `403` specifically (not 401/network/404, which keep their existing handling) and Basic Auth credentials are configured; otherwise the original error is returned with a hint to add credentials in Settings. **Two corrections found during the required live check, before wiring anything in** (see `CLAUDE-CONCEPT.md` section 7.4 for full detail):
  - The floor-matching rule in the original spec (match the floor whose `name` equals the site's `name`) **never holds** — checked against all 3 seeded sites, a floor's `name` always mirrors its own `code` (e.g. `TTMEMBASE_FLORR`'s name is `TTMEMBASE`), never the premise's display name (`T&TMembase`). The real, reliable link is structural: a floor's `parent` field holds its premise's numeric `id`. Implemented as `findFloorForPremiseCode()` (supersedes the originally-planned `findFloorByName()`).
  - The `/maps` endpoint returns **raw image bytes** (`Content-Type: image/png`, confirmed against a real 5718×3457 floor plan), not JSON metadata with a URL as first assumed. Since the browser can't hold the Basic Auth credentials, added a new streaming route `GET /api/locations/[code]/legacy-map-image` that fetches server-side and proxies the bytes; `LocationMapCard`'s `<img src>` points there. `LocationMap.width`/`height` made optional — the fallback path omits them, so the `<img>` renders at its natural pixel size, which is actually required for the zone/device marker overlay (positioned in raw pixel coordinates) to line up correctly.
  - Live-verified end-to-end in the browser against `TTMEMBASE`: the map card renders the real floor plan with zone/device markers correctly positioned, where it previously showed the `403` error state.

## 9. Overview / Homepage

Redesign decided 2026-08-27 per Luc's explicit request — see `CLAUDE-CONCEPT.md` section 14 for the full spec, including the three product decisions made with him: a floor-plan-based map (not a geographic map), `location-api-v2` as the source of truth for site data (see section 8 above), and a minimal Device model (BL-036) built now rather than deferred to BL-003. Replaces the previous static-KPI-only Overview page.

- [x] **BL-037** — Done 2026-08-27: `SiteSelectorCard.tsx` — globe icon, selected site name (large, clickable) + city/state/country beneath, dropdown listing every other Location (styled to match the avatar-menu hover pattern from BL-030). `POST /api/settings/selected-location` persists the choice to `User.selectedLocationCode`, verified round-trip (save → reload → correct value read back). **Currently shows its error state** rather than a working site list — `GET /api/locations` fails because the underlying `location-api-v2` call doesn't authenticate yet (see BL-002/section 8's live-test note above) — verified the error surfaces exactly as expected (the same `"apikey or basic auth is required"` message from the raw gateway call), not a crash or silent failure. Will start listing real sites the moment the gateway auth question is resolved, no code change needed.
- [x] **BL-038** — Done 2026-08-27: `LocationMapCard.tsx` — `.panel`-based card, CSS transform (`scale`/`translate`) pan-zoom layer, floating bottom-right control widget (zoom in/out/pan toggle, styled like the BL-031 sidebar icon buttons), Zone dots and Device markers (`ReadPointIcon`) positioned at raw pixel coordinates inside the transform layer (scales correctly with it at any zoom level, no manual math needed). No-floor-plan state and "no site selected" state both use the existing `.placeholder` pattern. `GET /api/locations/[code]/map` and `/zones` verified working correctly against the local dev server (proxy, auth, error-shape parsing all confirmed) — they return the gateway's real error cleanly rather than crashing, same as BL-037, since the upstream API isn't authenticating yet. Genuine floor-plan/marker rendering isn't visually verified against real data for the same reason — the mechanics (scale math, marker positioning) are implemented per spec and code-reviewed, not pixel-tested live.
- [x] **BL-038a** — Done 2026-08-28, direct request from Luc after seeing real floor plans render (BL-040/041): the map loaded at native pixel size (floor plans run several thousand pixels wide), so most of it was off-screen by default. `LocationMapCard.tsx` now computes a "fit to card" scale from the loaded `<img>`'s natural dimensions vs. the viewport's actual size on load, applied as both the initial view and the zoom-out floor (`minScale`, replacing the old fixed `MIN_SCALE`) — so "zoom out" always stops exactly at "whole map visible," never further. `MAX_SCALE` raised 4 → 6 for finer detail zoom. Added a fourth control button ("Fit to screen") to recenter/refit after panning or zooming. Live-verified against `TTMEMBASE`.
- [x] **BL-038b** — Done 2026-08-28, follow-up request from Luc: the fit computed on load went stale on any later resize of the card's own container (browser window resize, or this app's own sidebar collapse/expand). Added a `ResizeObserver` on the map viewport that re-runs the same fit calculation whenever its size changes. Verified live via the sidebar collapse toggle (a real, reproducible container-resize trigger in this app) — the `resize_window` browser-automation tool itself couldn't be used to verify a true OS window resize in this environment (`window.innerWidth` didn't change across calls), but the underlying mechanism is identical either way since `ResizeObserver` watches the element's own box size, not the window.
- [x] **BL-039** — Done 2026-08-27: Devices-online KPI now reads live from BL-036's `Device` model, scoped to the selected site (X online / Y total, or "no site selected" until one resolves) — verified against the seeded `TTMEMBASE` fixtures. Workflows/Serialized-items cards unchanged. Bug reports card removed.
- [x] **BL-048** — Done 2026-08-28, direct request from Luc (number picked to avoid colliding with the BL-042–047 Devices/Collectors items added the same day): on Overview, if any of the four Bartender credential fields (tenant URL, API key, Track & Trace username, password) aren't set, a "Connect to Bartender Track and Trace" popup (`ConnectBartenderModal.tsx`, reusing the existing `.modal-overlay`/`.modal` pattern from `BugReportModal.tsx`) appears with a "Go to Settings" button linking straight to `/settings` — the Bartender Connection tab is already the default tab there, so no query-param/tab-routing plumbing was needed. Dismissible ("Not now" / close), not a hard block — the check runs once per Overview load via `GET /api/settings/bartender`. Live-verified both states: shows when a field is cleared, silent when everything's configured.
