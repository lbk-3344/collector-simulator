import PendingPage from "./PendingPage";

export const metadata = {
  title: "Waiting for approval — Bartender Track and Trace Simulator",
};

export default function Page() {
  return <PendingPage />;
}
