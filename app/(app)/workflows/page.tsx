export default function WorkflowsPage() {
  return (
    <section className="fade-in placeholder">
      <span className="tag">Workflows</span>
      <h2>Not built yet</h2>
      <p>
        Workflow builder and run history — see <code>BACKLOG.md</code> section 6.
      </p>
    </section>
  );
}
