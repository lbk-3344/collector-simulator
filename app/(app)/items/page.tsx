export default function ItemsPage() {
  return (
    <section className="fade-in placeholder">
      <span className="tag">Serialized Items</span>
      <h2>Not built yet</h2>
      <p>
        Serialized item log, with/without label — see <code>BACKLOG.md</code> section 7.
      </p>
    </section>
  );
}
