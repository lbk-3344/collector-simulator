# Reported bugs (OPEN status)

Regenerated by hand on 2026-08-31 (the `npm run bugs:export` script — BL-009 — is still not built). Source: `OPEN` `BugReport` rows in the **production** database.

No open bugs.

_5 bugs reported 2026-08-31 (#cmth55eoh, #cmth579bx, #cmth5k10o, #cmth5mgqy, #cmth5n026) were fixed together in v0.17.2 and moved to `SOLVED-BUGS.md`. Their rows are kept in the production DB as `RESOLVED` (not deleted) because the `bugs:notify-resolved` step — BL-010 — which normally emails the reporter and drops the row, does not exist yet._
