# Reported bugs (OPEN status)

No export script yet (`npm run bugs:export` — see `BACKLOG.md` BL-009). This file will be regenerated on demand once the `BugReport` model and export script are built — no continuous sync, so always regenerate before starting bug work.

No open bugs (nothing built yet to report against).
