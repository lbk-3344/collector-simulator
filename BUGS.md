# Reported bugs (OPEN status)

Regenerated by hand on 2026-08-31. From v0.18.0 on this file is written by `npm run bugs:export` (BL-009), reading the `OPEN` `BugReport` rows in `DATABASE_URL_PRODUCTION`. It stays hand-maintained only until that release reaches production — the `bugs:*` scripts need the `BugReport.number` column added in the same release.

No open bugs.

_5 bugs reported 2026-08-31 (#cmth55eoh, #cmth579bx, #cmth5k10o, #cmth5mgqy, #cmth5n026) were fixed in v0.17.2 and archived to `SOLVED-BUGS.md`. Their rows are `RESOLVED` in the production DB, awaiting `npm run bugs:notify-resolved` once v0.17.2+ is live in production — that emails each reporter and deletes the row._
